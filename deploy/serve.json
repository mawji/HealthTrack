{
  "_comment": "Tailscale Serve config — HTTPS on 443 proxied to the app on 127.0.0.1:3210. ${TS_CERT_DOMAIN} is expanded by the tailscale container to this node's FQDN. You do not edit this file.",
  "TCP": {
    "443": { "HTTPS": true }
  },
  "Web": {
    "${TS_CERT_DOMAIN}:443": {
      "Handlers": {
        "/": { "Proxy": "http://127.0.0.1:3210" }
      }
    }
  }
}
